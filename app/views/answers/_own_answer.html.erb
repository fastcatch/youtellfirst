<div class="page-header"><h1>
  <small><%= @question.user == current_user ? 'You' : @question.user.name %> asked</small> <%= @question.text %> <small><%= time_ago_in_words(@question.created_at) %> ago</small>
</h1></div>

<p>You have answered this question:</p>
<p><%= @answer.text %></p>

<% if @question.pending? %>
  <p>There are other pending answer(s).  When everybody answered you will get an automatic e-mail notification.</p>
<% end -%>
